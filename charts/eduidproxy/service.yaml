apiVersion: v1
kind: Service
metadata:
  name: eduid-invite-proxy
  namespace: "{{ .Release.Namespace }}"
spec:
  selector:
    app: eduid-invite-proxy
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP